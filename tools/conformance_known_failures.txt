# Case names (conformance test stem) expected to fail in pycroscope.
# If a listed case starts passing, or an unlisted case starts failing,
# the conformance CI workflow should fail and this file should be updated.

# Doesn't detect when you use a TypeVar in the wrong place
generics_syntax_scoping

# Variadic arguments do not yet account for defaulted trailing ParamSpecs
generics_defaults

# Fails to reject "x" | int annotations that fail at runtime
# Does not correctly pick up forward refs in unimportable module
annotations_forward_refs

# We don't really do stubs
overloads_definitions_stub

# Too precise value for a global
protocols_generic
overloads_basic

# Abstract type[Proto] still allows protocol class objects in some paths.
protocols_class_objects

# Never/Any assignability
specialtypes_never

# Fails to reject various weird annotations
aliases_type_statement
aliases_implicit
annotations_typeexpr
typeforms_typeform
literals_parameterizations
qualifiers_annotated

# https://github.com/python/typing/issues/2259
dataclasses_descriptors
